# TODOリスト（検討事項）

本プロジェクトにおける、機能追加、技術的課題、未確定事項の一覧です。

## 1. 技術面 (Technical)

### [ ] GraalPy と Java のシームレスな統合
- **概要**: Java コードから既存の Ansible Python モジュールを効率的に呼び出すためのブリッジ設計。
- **検討内容**:
    - Polyglot API を用いたデータ型変換の最適化。
    - Python 側の標準ライブラリ（`ansible.module_utils` 等）の読み込み。

### [ ] SnakeYAML 2.x による Playbook 解析の実装
- **概要**: YAML 形式の Playbook を Java オブジェクト（Record）へマッピング。
- **検討内容**:
    - Ansible 独自のタグ（`!vault` 等）の処理方法。
    - 複雑なデータ構造（リストとディクショナリの混在）のパース。

## 2. 機能面 (Features)

### [ ] インベントリ管理の基本機能
- **概要**: ターゲットホストの静的ファイル（INI/YAML）からの読み込み。
- **検討内容**:
    - 静的ホストグループのサポート。
    - 階層化されたグループ変数の優先順位解決。

### [ ] 主要モジュールの Java による再実装
- **概要**: 高頻度で使用されるモジュールの高速化（Python 不要化）。
- **検討内容**:
    - `command`, `shell`, `file`, `template` モジュールの優先実装。

## 3. 実装時の詳細事項

### [ ] Native Image 時のリフレクション設定
- **概要**: YAML 解析や動的クラスロードに伴うリフレクション定義の生成。
- **検討内容**:
    - `reflect-config.json` の自動生成ツールの検討。

## 4. 人間が主導すべき設計・調整面

### [✓] Ansible 互換性の維持レベル
- **概要**: どのバージョンの Ansible と、どの程度の互換性を持つべきか。
- **決定事項**: **Ansible 13** で動くコレクションが動作することを目標とする。

## 完了済みの項目 (Completed)

### [x] CLI エントリポイントの実装
- **完了日**: 2026-02-28
- **概要**: `ansible-playbook` 互換の引数を受け付ける CLI の実装。
- **解決策**: [CLI仕様](features/CLI-Specification.md) に基づき、`picocli` を用いて `Main.java` および `PlaybookCli.java` を実装。

---
*更新日: 2026-02-28*
